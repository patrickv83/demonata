language: python
before_script:
  - pip install -r requirements.txt

script:
  - python -m pytest --cov=src -v
  - if [ -n "`git diff --name-only $TRAVIS_COMMIT_RANGE | grep .py`" ] ; 
    then pylint "`git diff --name-only $TRAVIS_COMMIT_RANGE | grep .py`";
    fi
after_success:
  - coveralls

notifications:
    slack: 4398softwareenggroup:9qpjHnEz4rWIRbq6AHynqSsj
